import styled from "styled-components";

import ClipLoader from "react-spinners/ClipLoader";

export const ContainerLoading = styled.div`

`;

export const LoaderIcon = styled(ClipLoader).attrs({})`
  
`;